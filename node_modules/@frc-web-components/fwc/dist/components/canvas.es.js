import { r as _, n as r, E as F, i as L, x as j } from "../property-C4BPH-wS.js";
import { a as J, e as K } from "../query-BrpQj_Qi.js";
import { r as G } from "../state-Cn3aen3G.js";
import { g as Q, t as Z } from "../index-bL2hVBUF.js";
import { e as tt, i as et } from "../directive-B76A7YXI.js";
import { m as it } from "../directive-helpers-CDfQTKPA.js";
/**
 * @license
 * Copyright 2017 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
let rt;
function st(a) {
  return (t, i) => J(t, i, { get() {
    return (this.renderRoot ?? rt ?? (rt = document.createDocumentFragment())).querySelectorAll(a);
  } });
}
var ot = Object.defineProperty, T = (a, t, i, o) => {
  for (var e = void 0, s = a.length - 1, n; s >= 0; s--)
    (n = a[s]) && (e = n(t, i, e) || e);
  return e && ot(t, i, e), e;
};
class A extends _ {
  constructor() {
    super(...arguments), this.points = [], this.opacity = 1, this.color = null, this.lineWidth = 2, this.transform = [0, 0, 0], this.transformOrigin = [0, 0], this.drawOrigin = ["left", "top"], this.zIndex = 0, this._points = [];
  }
  updated(t) {
    t.has("points") && (this._points = Q(this.points, 2));
  }
  draw({ ctx: t }) {
    t.globalAlpha = Math.max(0, Math.min(1, this.opacity)), t.lineWidth = this.lineWidth ?? 2, t.strokeStyle = this.color ?? "gray";
    const [i, o, e] = this.transform ?? [
      0,
      0,
      0
    ];
    t.translate(i, o);
    const [s, n] = this.transformOrigin ?? [0, 0], d = s, l = n;
    if (t.translate(d, l), t.rotate(-(e * Math.PI) / 180), t.translate(-d, -l), this._points.length > 1) {
      for (let h = 0; h < this._points.length - 1; h += 1) {
        const [I, p] = this._points[h], [c, W] = this._points[h + 1];
        t.moveTo(I, p), t.lineTo(c, W);
      }
      t.stroke();
    }
  }
}
T([
  r({ type: Array })
], A.prototype, "points");
T([
  r({ type: Number })
], A.prototype, "opacity");
T([
  r({ type: String })
], A.prototype, "color");
T([
  r({ type: Number, attribute: "line-width" })
], A.prototype, "lineWidth");
T([
  r({ type: Array })
], A.prototype, "transform");
T([
  r({ type: Array, attribute: "transform-origin" })
], A.prototype, "transformOrigin");
T([
  r({ type: Array, attribute: "draw-origin" })
], A.prototype, "drawOrigin");
T([
  r({ type: Array, attribute: "z-index" })
], A.prototype, "zIndex");
T([
  G()
], A.prototype, "_points");
customElements.get("frc-canvas-line") || customElements.define("frc-canvas-line", A);
var nt = Object.defineProperty, v = (a, t, i, o) => {
  for (var e = void 0, s = a.length - 1, n; s >= 0; s--)
    (n = a[s]) && (e = n(t, i, e) || e);
  return e && nt(t, i, e), e;
};
class u extends _ {
  constructor() {
    super(...arguments), this.origin = [0, 0], this.width = 10, this.height = 10, this.radii = 0, this.opacity = 1, this.strokeColor = null, this.strokeWidth = 2, this.fillColor = "gray", this.transform = [0, 0, 0], this.transformOrigin = [0, 0], this.drawOrigin = ["center", "center"], this.zIndex = 0;
  }
  draw({ ctx: t }) {
    t.globalAlpha = Math.max(0, Math.min(1, this.opacity)), this.fillColor && (t.fillStyle = this.fillColor), this.strokeColor && (t.strokeStyle = this.strokeColor, t.lineWidth = this.strokeWidth);
    let [i, o] = this.origin;
    const [e, s] = this.drawOrigin ?? ["center", "center"];
    e === "center" ? i -= this.width / 2 : e === "right" && (i -= this.width), s === "center" ? o -= this.height / 2 : s === "bottom" && (o -= this.height);
    const [n, d, l] = this.transform ?? [
      0,
      0,
      0
    ];
    i += n, o += d, t.translate(i, o);
    const [h, I] = this.transformOrigin ?? [0, 0], p = h + this.width / 2, c = I + this.height / 2;
    t.translate(p, c), t.rotate(-(l * Math.PI) / 180), t.translate(-p, -c), t.roundRect(0, 0, this.width, this.height, this.radii), this.fillColor && t.fill(), this.strokeColor && t.stroke();
  }
}
v([
  r({ type: Array })
], u.prototype, "origin");
v([
  r({ type: Number })
], u.prototype, "width");
v([
  r({ type: Number })
], u.prototype, "height");
v([
  r({ type: Number })
], u.prototype, "radii");
v([
  r({ type: Number })
], u.prototype, "opacity");
v([
  r({ type: String, attribute: "stroke-color" })
], u.prototype, "strokeColor");
v([
  r({ type: Number, attribute: "stroke-width" })
], u.prototype, "strokeWidth");
v([
  r({ type: String, attribute: "fill-color" })
], u.prototype, "fillColor");
v([
  r({ type: Array })
], u.prototype, "transform");
v([
  r({ type: Array, attribute: "transform-origin" })
], u.prototype, "transformOrigin");
v([
  r({ type: Array, attribute: "draw-origin" })
], u.prototype, "drawOrigin");
v([
  r({ type: Array, attribute: "z-index" })
], u.prototype, "zIndex");
customElements.get("frc-canvas-rect") || customElements.define("frc-canvas-rect", u);
var at = Object.defineProperty, C = (a, t, i, o) => {
  for (var e = void 0, s = a.length - 1, n; s >= 0; s--)
    (n = a[s]) && (e = n(t, i, e) || e);
  return e && at(t, i, e), e;
};
class y extends _ {
  constructor() {
    super(...arguments), this.origin = [0, 0], this.radius = 10, this.startAngle = 0, this.endAngle = 360, this.opacity = 1, this.strokeColor = null, this.strokeWidth = 2, this.fillColor = "gray", this.transform = [0, 0, 0], this.transformOrigin = [0, 0], this.drawOrigin = ["center", "center"], this.zIndex = 0;
  }
  draw({ ctx: t }) {
    t.globalAlpha = Math.max(0, Math.min(1, this.opacity)), this.fillColor && (t.fillStyle = this.fillColor), this.strokeColor && (t.strokeStyle = this.strokeColor, t.lineWidth = this.strokeWidth);
    let [i, o] = this.origin;
    const [e, s] = this.drawOrigin ?? ["center", "center"];
    e === "left" ? i += this.radius : e === "right" && (i -= this.radius), s === "top" ? o += this.radius : s === "bottom" && (o -= this.radius);
    const [n, d, l] = this.transform ?? [
      0,
      0,
      0
    ];
    i += n, o += d, t.translate(i, o);
    const [h, I] = this.transformOrigin ?? [0, 0], p = h, c = I;
    t.translate(p, c), t.rotate(-(l * Math.PI) / 180), t.translate(-p, -c), t.beginPath(), t.arc(
      0,
      0,
      this.radius,
      -(this.startAngle ?? 0) * Math.PI / 180,
      -(this.endAngle ?? 360) * Math.PI / 180,
      !0
    ), this.fillColor && t.fill(), this.strokeColor && t.stroke();
  }
}
C([
  r({ type: Array })
], y.prototype, "origin");
C([
  r({ type: Number })
], y.prototype, "radius");
C([
  r({ type: Number, attribute: "start-angle" })
], y.prototype, "startAngle");
C([
  r({ type: Number, attribute: "end-angle" })
], y.prototype, "endAngle");
C([
  r({ type: Number })
], y.prototype, "opacity");
C([
  r({ type: String, attribute: "stroke-color" })
], y.prototype, "strokeColor");
C([
  r({ type: Number, attribute: "stroke-width" })
], y.prototype, "strokeWidth");
C([
  r({ type: String, attribute: "fill-color" })
], y.prototype, "fillColor");
C([
  r({ type: Array })
], y.prototype, "transform");
C([
  r({ type: Array, attribute: "transform-origin" })
], y.prototype, "transformOrigin");
C([
  r({ type: Array, attribute: "draw-origin" })
], y.prototype, "drawOrigin");
C([
  r({ type: Array, attribute: "z-index" })
], y.prototype, "zIndex");
customElements.get("frc-canvas-circle") || customElements.define("frc-canvas-circle", y);
var ht = Object.defineProperty, O = (a, t, i, o) => {
  for (var e = void 0, s = a.length - 1, n; s >= 0; s--)
    (n = a[s]) && (e = n(t, i, e) || e);
  return e && ht(t, i, e), e;
};
class k extends _ {
  constructor() {
    super(...arguments), this.origin = [0, 0], this.sides = 5, this.radius = 10, this.opacity = 1, this.strokeColor = null, this.strokeWidth = 2, this.fillColor = "gray", this.transform = [0, 0, 0], this.transformOrigin = [0, 0], this.drawOrigin = ["center", "center"], this.zIndex = 0;
  }
  draw({ ctx: t }) {
    t.globalAlpha = Math.max(0, Math.min(1, this.opacity)), this.fillColor && (t.fillStyle = this.fillColor), this.strokeColor && (t.strokeStyle = this.strokeColor, t.lineWidth = this.strokeWidth);
    let [i, o] = this.origin;
    const [e, s] = this.drawOrigin ?? ["center", "center"];
    e === "left" ? i += this.radius : e === "right" && (i -= this.radius), s === "top" ? o += this.radius : s === "bottom" && (o -= this.radius);
    const [n, d, l] = this.transform ?? [
      0,
      0,
      0
    ];
    i += n, o += d, t.translate(i, o);
    const [h, I] = this.transformOrigin ?? [0, 0], p = h, c = I;
    if (t.translate(p, c), t.rotate(-(l * Math.PI) / 180), t.translate(-p, -c), this.sides < 3) return;
    const W = Math.PI * 2 / this.sides;
    t.beginPath();
    for (let N = 0; N < this.sides; N += 1) {
      const x = N * W, M = this.radius * Math.cos(x), Y = this.radius * Math.sin(x);
      N === 0 ? t.moveTo(M, Y) : t.lineTo(M, Y);
    }
    t.closePath(), this.fillColor && t.fill(), this.strokeColor && t.stroke();
  }
}
O([
  r({ type: Array })
], k.prototype, "origin");
O([
  r({ type: Number })
], k.prototype, "sides");
O([
  r({ type: Number })
], k.prototype, "radius");
O([
  r({ type: Number })
], k.prototype, "opacity");
O([
  r({ type: String, attribute: "stroke-color" })
], k.prototype, "strokeColor");
O([
  r({ type: Number, attribute: "stroke-width" })
], k.prototype, "strokeWidth");
O([
  r({ type: String, attribute: "fill-color" })
], k.prototype, "fillColor");
O([
  r({ type: Array })
], k.prototype, "transform");
O([
  r({ type: Array, attribute: "transform-origin" })
], k.prototype, "transformOrigin");
O([
  r({ type: Array, attribute: "draw-origin" })
], k.prototype, "drawOrigin");
O([
  r({ type: Array, attribute: "z-index" })
], k.prototype, "zIndex");
customElements.get("frc-canvas-ngon") || customElements.define("frc-canvas-ngon", k);
var lt = Object.defineProperty, R = (a, t, i, o) => {
  for (var e = void 0, s = a.length - 1, n; s >= 0; s--)
    (n = a[s]) && (e = n(t, i, e) || e);
  return e && lt(t, i, e), e;
};
class D extends _ {
  constructor() {
    super(...arguments), this.transform = [0, 0, 0], this.transformOrigin = [0, 0], this.zIndex = 0;
  }
  draw(t) {
    const { ctx: i } = t, [o, e, s] = this.transform ?? [
      0,
      0,
      0
    ];
    i.translate(o, e);
    const [n, d] = this.transformOrigin ?? [0, 0], l = n, h = d;
    i.translate(l, h), i.rotate(-(s * Math.PI) / 180), i.translate(-l, -h), [...this.children].forEach((I) => {
      var c;
      i.save(), i.beginPath();
      const p = I;
      (c = p.draw) == null || c.call(p, t), i.restore();
    });
  }
}
R([
  r({ type: Array })
], D.prototype, "transform");
R([
  r({ type: Array, attribute: "transform-origin" })
], D.prototype, "transformOrigin");
R([
  r({ type: Array, attribute: "z-index" })
], D.prototype, "zIndex");
customElements.get("frc-canvas-group") || customElements.define("frc-canvas-group", D);
var dt = Object.defineProperty, w = (a, t, i, o) => {
  for (var e = void 0, s = a.length - 1, n; s >= 0; s--)
    (n = a[s]) && (e = n(t, i, e) || e);
  return e && dt(t, i, e), e;
};
class f extends _ {
  constructor() {
    super(...arguments), this.text = "", this.origin = [0, 0], this.maxWidth = null, this.fontName = "sans-serif", this.fontSize = 12, this.opacity = 1, this.strokeColor = null, this.strokeWidth = 2, this.fillColor = "gray", this.transform = [0, 0, 0], this.transformOrigin = [0, 0], this.drawOrigin = ["center", "center"], this.zIndex = 0;
  }
  draw({ ctx: t }) {
    t.globalAlpha = Math.max(0, Math.min(1, this.opacity ?? 1)), this.fillColor && (t.fillStyle = this.fillColor), this.strokeColor && (t.strokeStyle = this.strokeColor), t.textAlign = "center", t.textBaseline = "middle", t.font = `${this.fontSize ?? 12}px ${this.fontName || "sans-serif"}`;
    let [i, o] = this.origin ?? [0, 0];
    const { width: e, actualBoundingBoxAscent: s, actualBoundingBoxDescent: n } = t.measureText(this.text ?? ""), d = s + n, [l, h] = this.drawOrigin ?? ["center", "center"];
    l === "left" ? i += e / 2 : l === "right" && (i -= e / 2), h === "top" ? o += d / 2 : h === "bottom" && (o -= d / 2);
    const [I, p, c] = this.transform ?? [
      0,
      0,
      0
    ];
    i += I, o += p, t.translate(i, o);
    const [W, N] = this.transformOrigin ?? [0, 0], x = W, M = N;
    t.translate(x, M), t.rotate(-(c * Math.PI) / 180), t.translate(-x, -M), this.fillColor && t.fillText(this.text ?? "", 0, 0), this.strokeColor && (t.lineWidth = this.strokeWidth ?? 2, t.strokeText(this.text ?? "", 0, 0));
  }
}
w([
  r({ type: String })
], f.prototype, "text");
w([
  r({ type: Array })
], f.prototype, "origin");
w([
  r({ type: Number, attribute: "max-width" })
], f.prototype, "maxWidth");
w([
  r({ type: String, attribute: "font-name" })
], f.prototype, "fontName");
w([
  r({ type: Number, attribute: "font-size" })
], f.prototype, "fontSize");
w([
  r({ type: Number })
], f.prototype, "opacity");
w([
  r({ type: String, attribute: "stroke-color" })
], f.prototype, "strokeColor");
w([
  r({ type: Number, attribute: "stroke-width" })
], f.prototype, "strokeWidth");
w([
  r({ type: String, attribute: "fill-color" })
], f.prototype, "fillColor");
w([
  r({ type: Array })
], f.prototype, "transform");
w([
  r({ type: Array, attribute: "transform-origin" })
], f.prototype, "transformOrigin");
w([
  r({ type: Array, attribute: "draw-origin" })
], f.prototype, "drawOrigin");
w([
  r({ type: Array, attribute: "z-index" })
], f.prototype, "zIndex");
customElements.get("frc-canvas-text") || customElements.define("frc-canvas-text", f);
/**
 * @license
 * Copyright 2021 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
const pt = tt(class extends et {
  constructor() {
    super(...arguments), this.key = F;
  }
  render(a, t) {
    return this.key = a, t;
  }
  update(a, [t, i]) {
    return t !== this.key && (it(a), this.key = t), i;
  }
});
/**
 * @license
 * Copyright 2018 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
const $ = (a) => a ?? F;
var ct = Object.defineProperty, S = (a, t, i, o) => {
  for (var e = void 0, s = a.length - 1, n; s >= 0; s--)
    (n = a[s]) && (e = n(t, i, e) || e);
  return e && ct(t, i, e), e;
};
function gt(a, t) {
  const i = new URL(a);
  if (t.resolution) {
    let o = t.resolution.width;
    const e = t.resolution.height;
    o !== -1 && e !== -1 && o > 0 && e > 0 && (o % 2 !== 0 && (o += 1), i.searchParams.set(
      "resolution",
      `${Math.floor(o)}x${Math.floor(e)}`
    ));
  }
  return t.fps !== void 0 && t.fps !== -1 && t.fps > 0 && i.searchParams.set("fps", t.fps.toString()), t.quality !== void 0 && t.quality !== -1 && t.quality >= 0 && t.quality <= 100 && i.searchParams.set("compression", Math.floor(t.quality).toString()), i.toString();
}
const U = class U extends _ {
  constructor() {
    super(...arguments), this.src = "", this.width = null, this.height = null, this.origin = [0, 0], this.disabled = !1, this.hideCrosshair = !1, this.crosshairColor = "white", this.image = new Image(), this.connected = !1, this.throttleUpdateImage = Z(() => this.updateImage(), 5e3), this.onImageLoadBound = this.onImageLoad.bind(this), this.onImageErrorBound = this.onImageError.bind(this);
  }
  getImageSize(t) {
    const i = {
      width: this.width || t.width,
      height: this.height || t.height
    };
    return this.image.height / this.image.width * i.width > i.height ? {
      height: i.height,
      width: this.image.width / this.image.height * i.height
    } : {
      height: this.image.height / this.image.width * i.width,
      width: i.width
    };
  }
  draw({ ctx: t, canvas: i }) {
    const { width: o, height: e } = this.getImageSize(i), s = {
      width: this.width || i.width,
      height: this.height || i.height
    }, [n, d] = this.origin ?? [0, 0];
    if (t.translate(n, d), !!this.connected)
      try {
        const l = (s.width - o) / 2, h = (s.height - e) / 2;
        t.drawImage(this.image, l, h, o, e), this.hideCrosshair || (t.strokeStyle = this.crosshairColor || "white", t.lineWidth = 1, t.beginPath(), t.setLineDash([7, 7]), t.moveTo(l, h + e / 2), t.lineTo(l + o, h + e / 2), t.stroke(), t.beginPath(), t.setLineDash([5, 5]), t.moveTo(l + o / 2, h), t.lineTo(l + o / 2, h + e), t.stroke());
      } catch {
        this.throttleUpdateImage();
      }
  }
  disconnectedCallback() {
    super.disconnectedCallback(), clearTimeout(this.loadedTimeout), this.image.src = "";
  }
  connectedCallback() {
    super.connectedCallback(), this.throttleUpdateImage();
  }
  isImageLoaded() {
    return this.image.complete && this.image.naturalHeight !== 0;
  }
  updateImage() {
    if (clearTimeout(this.loadedTimeout), this.disabled)
      return;
    if (!this.isConnected) {
      this.setConnected(!1);
      return;
    }
    const i = {
      resolution: this.resolutionWidth && this.resolutionHeight && this.resolutionWidth !== -1 && this.resolutionHeight !== -1 ? { width: this.resolutionWidth, height: this.resolutionHeight } : void 0,
      fps: this.fps,
      quality: this.quality
    }, o = gt(this.src ?? "", i);
    this.isImageLoaded() && this.image.src === o || (this.loadedTimeout = setTimeout(() => {
      this.throttleUpdateImage();
    }, 5e3), this.image.removeEventListener("load", this.onImageLoadBound), this.image.removeEventListener("error", this.onImageErrorBound), this.image.src = "", this.image = new Image(), this.setConnected(!1), this.image.src = o, this.image.addEventListener("load", this.onImageLoadBound), this.image.addEventListener("error", this.onImageErrorBound));
  }
  setConnected(t) {
    this.connected && !t ? (this.connected = !1, this.dispatchDisconnect()) : !this.connected && t && (this.connected = !0, this.dispatchConnect());
  }
  onImageLoad() {
    this.setConnected(!0), clearTimeout(this.loadedTimeout);
  }
  onImageError() {
    this.throttleUpdateImage();
  }
  dispatchConnect() {
    this.dispatchEvent(
      new CustomEvent("connect", {
        bubbles: !0,
        composed: !0
      })
    );
  }
  dispatchDisconnect() {
    this.dispatchEvent(
      new CustomEvent("disconnect", {
        bubbles: !0,
        composed: !0
      })
    );
  }
  updated(t) {
    (t.has("src") || t.has("resolutionWidth") || t.has("resolutionHeight") || t.has("fps") || t.has("quality")) && this.throttleUpdateImage(), t.has("disabled") && (this.disabled ? (clearTimeout(this.loadedTimeout), this.image.src = "") : this.throttleUpdateImage());
  }
};
U.styles = L`
    :host {
      display: inline-block;
      width: 400px;
      height: 300px;
    }

    canvas {
      width: 100%;
      height: 100%;
    }
  `;
let g = U;
S([
  r({ type: String })
], g.prototype, "src");
S([
  r({ type: Number })
], g.prototype, "width");
S([
  r({ type: Number })
], g.prototype, "height");
S([
  r({ type: Array })
], g.prototype, "origin");
S([
  r({ type: Boolean })
], g.prototype, "disabled");
S([
  r({ type: Boolean, attribute: "hide-crosshair" })
], g.prototype, "hideCrosshair");
S([
  r({ type: String, attribute: "crosshair-color" })
], g.prototype, "crosshairColor");
S([
  r({ type: Number, attribute: "resolution-width" })
], g.prototype, "resolutionWidth");
S([
  r({ type: Number, attribute: "resolution-height" })
], g.prototype, "resolutionHeight");
S([
  r({ type: Number })
], g.prototype, "fps");
S([
  r({ type: Number })
], g.prototype, "quality");
customElements.get("frc-canvas-mjpg-stream-instance") || customElements.define(
  "frc-canvas-mjpg-stream-instance",
  g
);
var ft = Object.defineProperty, b = (a, t, i, o) => {
  for (var e = void 0, s = a.length - 1, n; s >= 0; s--)
    (n = a[s]) && (e = n(t, i, e) || e);
  return e && ft(t, i, e), e;
}, P;
const m = (P = class extends _ {
  constructor() {
    super(...arguments), this.srcs = [], this.width = null, this.height = null, this.origin = [0, 0], this.waitImage = P.DEFAULT_WAIT_IMAGE, this.hideCrosshair = !1, this.crosshairColor = "white", this.waitImageElement = new Image();
  }
  getImageSize(t) {
    const i = {
      width: this.width || t.width,
      height: this.height || t.height
    };
    return this.waitImageElement.height / this.waitImageElement.width * i.width > i.height ? {
      height: i.height,
      width: this.waitImageElement.width / this.waitImageElement.height * i.height
    } : {
      height: this.waitImageElement.height / this.waitImageElement.width * i.width,
      width: i.width
    };
  }
  draw(t) {
    const { canvas: i, ctx: o } = t, e = [...this.streamInstances].find(
      (s) => s.src === this._connectedSrc
    );
    if (e)
      e.draw(t);
    else {
      const { width: s, height: n } = this.getImageSize(i), d = {
        width: this.width || i.width,
        height: this.height || i.height
      }, [l, h] = this.origin ?? [0, 0];
      o.translate(l, h);
      try {
        o.drawImage(
          this.waitImageElement,
          (d.width - s) / 2,
          (d.height - n) / 2,
          s,
          n
        );
      } catch {
      }
    }
  }
  updated(t) {
    t.has("srcs") && this._connectedSrc && !this.srcs.includes(this._connectedSrc) && (this._connectedSrc = void 0), t.has("waitImage") && (this.waitImageElement.src = this.waitImage);
  }
  onConnect(t) {
    this._connectedSrc || (this._connectedSrc = t);
  }
  onDisconnect(t) {
    this._connectedSrc === t && (this._connectedSrc = void 0);
  }
  renderStreamInstance(t) {
    return j`
      ${pt(
      t,
      j`
          <frc-canvas-mjpg-stream-instance
            @connect=${() => this.onConnect(t)}
            @disconnect=${() => this.onDisconnect(t)}
            src=${t}
            width=${$(this.width !== null ? this.width : void 0)}
            height=${$(this.height !== null ? this.height : void 0)}
            .origin=${$(this.origin !== null ? this.origin : void 0)}
            ?disabled=${this._connectedSrc && this._connectedSrc !== t}
            ?hide-crosshair=${this.hideCrosshair}
            crosshair-color=${this.crosshairColor}
            resolution-width=${$(this.resolutionWidth)}
            resolution-height=${$(this.resolutionHeight)}
            fps=${$(this.fps)}
            quality=${$(this.quality)}
          ></frc-canvas-mjpg-stream-instance>
        `
    )}
    `;
  }
  render() {
    return j`
      ${(this.srcs ?? []).map(
      (t) => this.renderStreamInstance(t.replace("mjpg:", ""))
    )}
    `;
  }
}, P.DEFAULT_WAIT_IMAGE = "./no-camera-stream.jpg", P);
b([
  r({ type: Array })
], m.prototype, "srcs");
b([
  r({ type: Number })
], m.prototype, "width");
b([
  r({ type: Number })
], m.prototype, "height");
b([
  r({ type: Array })
], m.prototype, "origin");
b([
  r({ type: String, attribute: "wait-image" })
], m.prototype, "waitImage");
b([
  r({ type: Boolean, attribute: "hide-crosshair" })
], m.prototype, "hideCrosshair");
b([
  r({ type: String, attribute: "crosshair-color" })
], m.prototype, "crosshairColor");
b([
  r({ type: Number, attribute: "resolution-width" })
], m.prototype, "resolutionWidth");
b([
  r({ type: Number, attribute: "resolution-height" })
], m.prototype, "resolutionHeight");
b([
  r({ type: Number })
], m.prototype, "fps");
b([
  r({ type: Number })
], m.prototype, "quality");
b([
  G()
], m.prototype, "_connectedSrc");
b([
  st("frc-canvas-mjpg-stream-instance")
], m.prototype, "streamInstances");
let V = m;
customElements.get("frc-canvas-mjpg-stream") || customElements.define("frc-canvas-mjpg-stream", V);
var mt = Object.defineProperty, q = (a, t, i, o) => {
  for (var e = void 0, s = a.length - 1, n; s >= 0; s--)
    (n = a[s]) && (e = n(t, i, e) || e);
  return e && mt(t, i, e), e;
};
const X = class X extends _ {
  constructor() {
    super(...arguments), this.width = null, this.backgroundColor = "black";
  }
  getCanvasCtx() {
    return this.canvas.getContext("2d");
  }
  drawChildren() {
    const t = {
      canvas: this.canvas,
      ctx: this.getCanvasCtx(),
      domRect: this.getBoundingClientRect()
    };
    [...this.children].forEach((i) => {
      var s;
      const o = this.getCanvasCtx();
      o.save(), o.beginPath();
      const e = i;
      (s = e.draw) == null || s.call(e, t), o.restore();
    });
  }
  drawBackground() {
    const t = this.getCanvasCtx();
    t.save(), t.fillStyle = this.backgroundColor, t.fillRect(0, 0, this.canvas.width, this.canvas.height), t.restore();
  }
  drawCanvas() {
    this.drawBackground(), this.drawChildren(), window.requestAnimationFrame(() => {
      this.drawCanvas();
    });
  }
  firstUpdated() {
    this.drawCanvas(), new ResizeObserver(() => this.resized()).observe(this), this.resized();
  }
  resized() {
    const t = this.getBoundingClientRect(), { width: i, height: o } = t;
    typeof this.width != "number" ? (this.canvas.width = i, this.canvas.height = o) : (this.canvas.width = this.width, this.canvas.height = this.width * o / i);
  }
  render() {
    return j` <canvas></canvas> `;
  }
};
X.styles = L`
    :host {
      display: inline-block;
      width: 400px;
      height: 300px;
    }

    canvas {
      width: 100%;
      height: 100%;
    }

    [part='box'] {
      width: 100%;
      height: 100%;
      display: flex;
      justify-content: center;
      align-content: center;
      flex-wrap: wrap;
      background-color: var(--box-color);
      text-align: center;
    }
  `;
let z = X;
q([
  K("canvas")
], z.prototype, "canvas");
q([
  r({ type: Number })
], z.prototype, "width");
q([
  r({ type: String, attribute: "background-color" })
], z.prototype, "backgroundColor");
customElements.get("frc-canvas") || customElements.define("frc-canvas", z);
var ut = Object.defineProperty, B = (a, t, i, o) => {
  for (var e = void 0, s = a.length - 1, n; s >= 0; s--)
    (n = a[s]) && (e = n(t, i, e) || e);
  return e && ut(t, i, e), e;
};
const It = {
  dashboard: {
    displayName: "Camera"
  },
  properties: {
    streams: { type: "Array" },
    backgroundColor: {
      type: "String",
      attribute: "background-color",
      defaultValue: "#000000",
      input: { type: "ColorPicker" }
    },
    waitImage: {
      type: "String",
      attribute: "wait-image"
      // input: { type: 'Upload' },
    },
    hideCrosshair: {
      type: "Boolean",
      attribute: "hide-crosshair"
    },
    crosshairColor: {
      type: "String",
      attribute: "crosshair-color",
      defaultValue: "#ffffff",
      input: { type: "ColorPicker" }
    }
  }
}, H = class H extends _ {
  constructor() {
    super(...arguments), this.streams = [], this.backgroundColor = "black", this.waitImage = V.DEFAULT_WAIT_IMAGE, this.hideCrosshair = !1, this.crosshairColor = "white";
  }
  render() {
    return j`
      <frc-canvas background-color=${this.backgroundColor}>
        <frc-canvas-mjpg-stream
          wait-image=${this.waitImage}
          crosshair-color=${this.crosshairColor}
          ?hide-crosshair=${this.hideCrosshair}
          .srcs=${this.streams}
        ></frc-canvas-mjpg-stream>
      </frc-canvas>
    `;
  }
};
H.styles = L`
    :host {
      display: inline-block;
      width: 400px;
      height: 300px;
    }

    frc-canvas {
      width: 100%;
      height: 100%;
    }
  `;
let E = H;
B([
  r({ type: Array })
], E.prototype, "streams");
B([
  r({ type: String, attribute: "background-color" })
], E.prototype, "backgroundColor");
B([
  r({ type: String, attribute: "wait-image" })
], E.prototype, "waitImage");
B([
  r({ type: Boolean, attribute: "hide-crosshair" })
], E.prototype, "hideCrosshair");
B([
  r({ type: String, attribute: "crosshair-color" })
], E.prototype, "crosshairColor");
customElements.get("frc-camera-wrapper") || customElements.define("frc-camera-wrapper", E);
export {
  E as CameraWrapper,
  z as Canvas,
  y as CanvasCircle,
  D as CanvasGroup,
  A as CanvasLine,
  V as CanvasMjpgStream,
  k as CanvasNGon,
  u as CanvasRect,
  f as CanvasText,
  It as cameraDashboardConfig,
  z as default
};
