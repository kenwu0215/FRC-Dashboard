import { WebbitConfig } from '@webbitjs/webbit';
import { NumberBar } from '../number-bar';
export declare const accelerometerDashboardConfig: Partial<WebbitConfig>;
/**
 * A component for displaying accelerometer data.
 */
export declare class Accelerometer extends NumberBar {
    value: number;
    min: number;
    max: number;
    center: number;
    precision: number;
    hideText: boolean;
    numTickMarks: number;
    unit: string;
}
export default Accelerometer;
declare global {
    interface HTMLElementTagNameMap {
        'frc-accelerometer': Accelerometer;
    }
}
