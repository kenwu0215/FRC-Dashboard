import { LitElement, TemplateResult } from 'lit';
import { CanvasObjectApi } from './interfaces';
export default class CanvasMjpgStream extends LitElement {
    static DEFAULT_WAIT_IMAGE: string;
    srcs: string[];
    width: number | null;
    height: number | null;
    origin: [number, number];
    waitImage: string;
    hideCrosshair: boolean;
    crosshairColor: string;
    resolutionWidth?: number;
    resolutionHeight?: number;
    fps?: number;
    quality?: number;
    _connectedSrc?: string;
    streamInstances: NodeListOf<HTMLElement>;
    private waitImageElement;
    private getImageSize;
    protected draw(api: CanvasObjectApi): void;
    protected updated(changedProps: Map<string, unknown>): void;
    private onConnect;
    private onDisconnect;
    private renderStreamInstance;
    render(): TemplateResult;
}
declare global {
    interface HTMLElementTagNameMap {
        'frc-canvas-mjpg-stream': CanvasMjpgStream;
    }
}
