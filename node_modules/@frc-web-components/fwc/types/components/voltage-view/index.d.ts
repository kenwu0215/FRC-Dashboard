import { WebbitConfig } from '@webbitjs/webbit';
import { NumberBar } from '../number-bar';
export declare const voltageViewDashboardConfig: Partial<WebbitConfig>;
export declare class VoltageView extends NumberBar {
    value: number;
    min: number;
    max: number;
    center: number;
    precision: number;
    hideText: boolean;
    numTickMarks: number;
    unit: string;
    static styles: import('lit').CSSResult[];
}
export default VoltageView;
declare global {
    interface HTMLElementTagNameMap {
        'frc-voltage-view': VoltageView;
    }
}
