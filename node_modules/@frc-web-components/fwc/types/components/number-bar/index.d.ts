import { LitElement } from 'lit';
import { WebbitConfig } from '@webbitjs/webbit';
export declare const numberBarDashboardConfig: Partial<WebbitConfig>;
export declare class NumberBar extends LitElement {
    value: number;
    min: number;
    max: number;
    center: number;
    precision: number;
    hideText: boolean;
    numTickMarks: number;
    unit: string;
    static styles: import('lit').CSSResult[];
    render(): import('lit').TemplateResult<1>;
}
export default NumberBar;
declare global {
    interface HTMLElementTagNameMap {
        'frc-number-bar': NumberBar;
    }
}
