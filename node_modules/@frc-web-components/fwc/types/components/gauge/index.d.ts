import { WebbitConfig } from '@webbitjs/webbit';
import { LitElement } from 'lit';
import { GaugeInstance } from 'svg-gauge';
export declare const gaugeDashboardConfig: Partial<WebbitConfig>;
export declare class Gauge extends LitElement {
    min: number;
    max: number;
    value: number;
    gaugeElement: HTMLElement;
    gauge?: GaugeInstance;
    static styles: import('lit').CSSResult;
    setSize(): void;
    gaugeInit(): void;
    firstUpdated(): void;
    updated(changedProperties: Map<string, unknown>): void;
    render(): import('lit').TemplateResult<1>;
}
export default Gauge;
declare global {
    interface HTMLElementTagNameMap {
        'frc-gauge': Gauge;
    }
}
