import { LitElement } from 'lit';
import { WebbitConfig } from '@webbitjs/webbit';
export declare const relayDashboardConfig: Partial<WebbitConfig>;
declare const RELAY_OPTIONS: readonly ["Off", "On", "Forward", "Reverse"];
type RelayOption = (typeof RELAY_OPTIONS)[number];
export declare class Relay extends LitElement {
    #private;
    value: RelayOption;
    direction: string;
    static styles: import('lit').CSSResult;
    render(): import('lit').TemplateResult<1>;
}
export default Relay;
declare global {
    interface HTMLElementTagNameMap {
        'frc-relay': Relay;
    }
}
