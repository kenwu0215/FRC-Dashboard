import { WebbitConfig } from '@webbitjs/webbit';
import { LitElement, TemplateResult } from 'lit';
import { Source } from '@webbitjs/store';
import { Mechanism2dRoot } from './mechanism2d';
export declare const mechanism2dDashboardConfig: Partial<WebbitConfig>;
export declare class Mechanism2dWrapper extends LitElement {
    backgroundColor: string;
    dims: [number, number];
    source: Source | undefined;
    roots: Mechanism2dRoot[];
    static styles: import('lit').CSSResult;
    updateMechanism2d(): void;
    firstUpdated(): void;
    render(): TemplateResult;
}
export default Mechanism2dWrapper;
declare global {
    interface HTMLElementTagNameMap {
        'frc-mechanism2d-wrapper': Mechanism2dWrapper;
    }
}
