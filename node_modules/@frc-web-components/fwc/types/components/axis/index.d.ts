import { LitElement } from 'lit';
import { WebbitConfig } from '@webbitjs/webbit';
export declare const axisDashboardConfig: Partial<WebbitConfig>;
export declare class Axis extends LitElement {
    vertical: boolean;
    ticks: number;
    min: number;
    max: number;
    unit: string;
    prevSize: number;
    prevTicks: number;
    prevMin: number | null;
    prevMax: number | null;
    svg: HTMLElement;
    static styles: import('lit').CSSResult;
    setAxis(changedProps: Map<string, unknown>): void;
    firstUpdated(): void;
    updated(changedProps: Map<string, unknown>): void;
    render(): import('lit').TemplateResult<1>;
}
export default Axis;
declare global {
    interface HTMLElementTagNameMap {
        'frc-axis': Axis;
    }
}
