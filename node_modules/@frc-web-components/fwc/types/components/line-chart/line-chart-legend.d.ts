import { LitElement } from 'lit';
export declare const ChartLegendPositions: readonly ["nw", "n", "ne", "w", "e", "sw", "s", "se"];
export type ChartLegendPosition = (typeof ChartLegendPositions)[number];
export type ChartLegendDirection = 'horizontal' | 'vertical';
export interface IChartLegend {
    position: ChartLegendPosition;
    direction: ChartLegendDirection;
    hide: boolean;
    inside: boolean;
}
export default class LineChartLegend extends LitElement implements IChartLegend {
    position: ChartLegendPosition;
    direction: ChartLegendDirection;
    hide: boolean;
    inside: boolean;
}
declare global {
    interface HTMLElementTagNameMap {
        'frc-line-chart-legend': LineChartLegend;
    }
}
