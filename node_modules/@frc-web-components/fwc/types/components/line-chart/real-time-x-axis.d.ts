import { TemplateResult } from 'lit';
import * as d3 from 'd3';
export declare function getXScale(width: number, totalTimeMs: number, elapsedTime: number): d3.ScaleTime<number, number, never>;
export default function getRealTimeXAxis(xScale: d3.ScaleTime<number, number, never>): TemplateResult;
export declare function getRealTimeXGrid(xScale: d3.ScaleTime<number, number, never>, height: number): TemplateResult;
