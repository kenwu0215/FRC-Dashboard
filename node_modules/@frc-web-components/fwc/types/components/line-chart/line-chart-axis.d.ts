import { LitElement } from 'lit';
export interface ILineChartAxis {
    min: number;
    max: number;
    lockMin: boolean;
    lockMax: boolean;
    invert: boolean;
    side: 'left' | 'right';
    hideGridLines: boolean;
    hide: boolean;
}
export default class LineChartAxis extends LitElement implements ILineChartAxis {
    min: number;
    max: number;
    lockMin: boolean;
    lockMax: boolean;
    invert: boolean;
    side: 'left' | 'right';
    hideGridLines: boolean;
    hide: boolean;
}
declare global {
    interface HTMLElementTagNameMap {
        'frc-line-chart-axis': LineChartAxis;
    }
}
