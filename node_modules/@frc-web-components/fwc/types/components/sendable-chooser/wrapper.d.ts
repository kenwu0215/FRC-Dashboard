import { LitElement, TemplateResult } from 'lit';
import { WebbitConfig } from '@webbitjs/webbit';
import { default as Store, SourceProvider } from '@webbitjs/store';
export declare const sendableChooserDashboardConfig: Partial<WebbitConfig>;
export declare class SendableChooserWrapper extends LitElement {
    #private;
    options: string[];
    selected: string;
    default: string;
    active: string;
    label: string;
    provider?: SourceProvider;
    store?: Store;
    sourceProvider: string;
    sourceKey: string;
    static styles: import('lit').CSSResult;
    updated(changedProps: Map<string, unknown>): void;
    render(): TemplateResult;
}
export default SendableChooserWrapper;
declare global {
    interface HTMLElementTagNameMap {
        'frc-sendable-chooser-wrapper': SendableChooserWrapper;
    }
}
