import { LitElement } from 'lit';
import { FieldObjectApi } from './field-interfaces';
export declare class FieldPath extends LitElement {
    poses: Uint8Array | number[] | (Uint8Array | number[])[];
    translations: Uint8Array | number[];
    color: string;
    unit: string | null;
    lineWidth: number;
    opacity: number;
    _poses: (Uint8Array | number[])[];
    _translations: (Uint8Array | number[])[];
    updated(changedProps: Map<string, unknown>): void;
    draw({ canvas, unit: parentUnit, xToPx, yToPx }: FieldObjectApi): void;
}
declare global {
    interface HTMLElementTagNameMap {
        'frc-field-path': FieldPath;
    }
}
