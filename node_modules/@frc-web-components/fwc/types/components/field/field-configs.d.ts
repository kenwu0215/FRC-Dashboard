export interface FieldConfig {
    game: string;
    image: string;
    corners: {
        topLeft: [number, number];
        bottomRight: [number, number];
    };
    size: [number, number];
    unit: string;
}
export declare const configs: FieldConfig[];
export default configs;
