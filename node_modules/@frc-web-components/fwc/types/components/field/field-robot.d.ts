import { LitElement } from 'lit';
import { FieldObjectApi } from './field-interfaces';
export declare class FieldRobot extends LitElement {
    unit: string | null;
    rotationUnit: string | null;
    image: string;
    color: string;
    opacity: number;
    pose: number[];
    width: number;
    length: number;
    draw({ canvas, unit: parentUnit, rotationUnit: parentRotationUnit, xToPx, yToPx, lengthToPx, origin, }: FieldObjectApi): void;
}
declare global {
    interface HTMLElementTagNameMap {
        'frc-field-robot': FieldRobot;
    }
}
