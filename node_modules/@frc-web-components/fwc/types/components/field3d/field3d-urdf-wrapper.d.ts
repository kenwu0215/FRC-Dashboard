import { LitElement, PropertyValueMap, TemplateResult } from 'lit';
import { default as Store, SourceProvider } from '@webbitjs/store';
import { WebbitConfig } from '@webbitjs/webbit';
export declare function getField3dUrdfDashboardConfig(urdfNames: string[]): WebbitConfig;
export default class Urdf3dWrapper extends LitElement {
    #private;
    name: string;
    pose: number[];
    provider?: SourceProvider;
    store?: Store;
    sourceProvider: string;
    sourceKey: string;
    getJointValues(): Record<string, number>;
    protected createRenderRoot(): this;
    protected updated(changedProps: PropertyValueMap<any> | Map<PropertyKey, unknown>): void;
    render(): TemplateResult;
}
declare global {
    interface HTMLElementTagNameMap {
        'frc-field3d-urdf-wrapper': Urdf3dWrapper;
    }
}
