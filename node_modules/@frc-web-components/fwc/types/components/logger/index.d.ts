import { LitElement } from 'lit';
import { WebbitConfig } from '@webbitjs/webbit';
import { default as HtmlLogger } from './html-logger';
export declare const loggerDashboardConfig: Partial<WebbitConfig>;
export default class Logger extends LitElement {
    title: string;
    maxLogCount: number;
    debug: string;
    info: string;
    success: string;
    warning: string;
    error: string;
    level: string;
    disabled: boolean;
    logger: HtmlLogger;
    levels: string[];
    static styles: import('lit').CSSResult;
    firstUpdated(): void;
    updated(changedProperties: Map<string, unknown>): void;
    render(): import('lit').TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        'frc-logger': Logger;
    }
}
