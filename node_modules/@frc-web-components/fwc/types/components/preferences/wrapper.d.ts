import { WebbitConfig } from '@webbitjs/webbit';
import { LitElement, TemplateResult, PropertyValueMap } from 'lit';
import { default as Store, Source, SourceProvider } from '@webbitjs/store';
export declare const preferencesDashboardConfig: Partial<WebbitConfig>;
export declare class PreferencesWrapper extends LitElement {
    #private;
    search: string;
    hideTitle: boolean;
    sourceKey: string;
    sourceProvider: string;
    source: Source | undefined;
    provider?: SourceProvider;
    store?: Store;
    static styles: import('lit').CSSResult;
    protected updated(changedProps: PropertyValueMap<any> | Map<PropertyKey, unknown>): void;
    disconnectedCallback(): void;
    render(): TemplateResult;
}
export default PreferencesWrapper;
declare global {
    interface HTMLElementTagNameMap {
        'frc-preferences-wrapper': PreferencesWrapper;
    }
}
