import { SourceProvider } from '@webbitjs/store';
export default class Nt4Provider extends SourceProvider {
    private serverAddress;
    private clients;
    private client?;
    private connected;
    private topics;
    private topicValues;
    private unprocessedUpdates;
    private connectionListeners;
    constructor();
    setValue(key: string, value: unknown): void;
    getValue<T>(key: string, defaultValue: T): T;
    getServerAddress(): string;
    connect(serverAddr: string): void;
    disconnect(): void;
    isConnected(): boolean;
    addConnectionListener(listener: (connected: boolean, serverAddress: string) => unknown, callImediately?: boolean): void;
    private processUnprocessedUpdates;
    userUpdate(key: string, value: unknown): void;
    private onTopicAnnounce;
    private onTopicUnannounce;
    private onNewTopicData;
    private onConnect;
    private onDisconnect;
    private createClient;
}
