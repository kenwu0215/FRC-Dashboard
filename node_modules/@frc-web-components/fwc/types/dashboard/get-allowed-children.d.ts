import { WebbitConfig, WebbitConnector } from '@webbitjs/webbit';
export declare function getAllowedChildrenByConfig(config: WebbitConfig, connector: WebbitConnector): {
    slot: string;
    allowedChildren: string[];
}[];
export default function getAllowedChildren(element: HTMLElement, connector: WebbitConnector): {
    slot: string;
    allowedChildren: string[];
}[];
